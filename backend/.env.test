# Test environment variables
ENVIRONMENT=test

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travelpal_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travelpal_test
ASYNC_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/travelpal_test

# Security
SECRET_KEY=test_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email (mock these in tests)
SMTP_TLS=True
SMTP_PORT=1025
SMTP_HOST=localhost
EMAILS_ENABLED=False
EMAIL_TEST_USER=test@example.com
EMAIL_FROM=test@example.com
EMAIL_FROM_NAME="Test Sender"

# First superuser (for testing)
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=password

# CORS
BACKEND_CORS_ORIGINS=["*"]

# OpenAI (mock this in tests)
OPENAI_API_KEY=test_key

# Redis (mock this in tests)
REDIS_URL=redis://test:test@localhost:6379/0
